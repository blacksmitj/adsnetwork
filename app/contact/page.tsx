import ContactClient from "./ContactClient";

export const metadata = {
  title: "ADS Network | Kontak",
  description: "Hubungi kami untuk info lebih lanjut",
};

const ContactPage = () => {
  return <ContactClient />;
};

export default ContactPage;
